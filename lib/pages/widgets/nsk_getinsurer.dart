import 'package:flutter/material.dart';

class GetInsurerWidget extends StatelessWidget {
  const GetInsurerWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}