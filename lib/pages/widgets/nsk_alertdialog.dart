import 'package:flutter/material.dart';

class NskAlertDialog extends StatelessWidget {
  const NskAlertDialog({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}